-- testing keyword being identified correctly

ClAsS Main{
	main():Int{0};
	flag:Bool <- tRUe;
	another_flag:Bool <- FaLse;
};
-- other keywords are case insensitive as shown in the second line (ClAsS)
-- error occurs at last boolean initialization, as true's and false's first letters have to be small 
