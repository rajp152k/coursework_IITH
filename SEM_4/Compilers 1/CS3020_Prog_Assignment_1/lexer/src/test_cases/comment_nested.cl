(* valid comment *)
class Main{main():Int{0};}; (* valid comment*)

(* parent comment (* usin
		     g non-greedy matching to not directly match the last comment *) *)

(* EOF criterion being tested here 
