# This program tests the priority of operators
fig figure{
	# TODO: We haven't specified the priorities yet
	x <- [1, 2, 3, 4]
	y := x^2 + 2*x + 1

	x <- [1, 2, 3, 4, 5]
	y <- [-1, -2, 3, -4, -5]
	z := x^2 + y^2 - 1 # ((x^2) + (y^2)) - 1
	z := -z

	x <- [1, 2, 3]
	y := x^2 * 2 # (x^2) * 2

	x <- [1, 2, 3]
	y := 2 * x / 3 # (2*x)/3
}